# Build outputs
build/
build_wasm/
*.so
*.o
*.a

# CMake generated
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
install_manifest.txt

# ImGui saved state
imgui.ini

# emsdk (too large, installed separately)
emsdk/

# Downloaded files (can be re-fetched)
# third_party is included (small headers only)
